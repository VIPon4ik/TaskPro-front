export enum InputType {
  Text = 'text',
  Password = 'password',
  Email = 'email',
}

export enum ButtonType {
  Button = 'button',
  Submit = 'submit',
}

export enum ButtonColor {
  Primary = 'primary',
  Secondary = 'secondary',
  Transparent = 'transparent',
  Danger = 'danger',
}
